services:
  ficibus-producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-ficibus
    environment:
      - "CONFIGURATION_PATH=/app/configurations/fecamp.mjs"
      - "TZ=Europe/Paris"
    ports:
      - "127.0.0.1:50005:3000"
    restart: unless-stopped

  amelys-producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-amelys
    environment:
      - "CONFIGURATION_PATH=/app/configurations/montargis.mjs"
      - "TZ=Europe/Paris"
    ports:
      - "127.0.0.1:50007:3000"
    restart: unless-stopped

  chateauroux-producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-chateauroux
    environment:
      - "CONFIGURATION_PATH=/app/configurations/chateauroux.mjs"
      - "TZ=Europe/Paris"
    ports:
      - "127.0.0.1:50010:3000"
    restart: unless-stopped

  niort-producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-niort
    environment:
      - "CONFIGURATION_PATH=/app/configurations/niort.mjs"
      - "TZ=Europe/Paris"
    ports:
      - "127.0.0.1:50011:3000"
    restart: unless-stopped

  dreux-producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-dreux
    environment:
      - "CONFIGURATION_PATH=/app/configurations/dreux.mjs"
      - "TZ=Europe/Paris"
    ports:
      - "127.0.0.1:50012:3000"
    restart: unless-stopped

  vichy-producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-vichy
    environment:
      - "CONFIGURATION_PATH=/app/configurations/vichy.mjs"
      - "TZ=Europe/Paris"
    ports:
      - "127.0.0.1:50013:3000"
    restart: unless-stopped

  morlaix-producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-morlaix
    environment:
      - "CONFIGURATION_PATH=/app/configurations/morlaix.mjs"
      - "TZ=Europe/Paris"
    ports:
      - "127.0.0.1:50014:3000"
    restart: unless-stopped

  vitre-producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-vitre
    environment:
      - "CONFIGURATION_PATH=/app/configurations/vitre.mjs"
      - "TZ=Europe/Paris"
    ports:
      - "127.0.0.1:50015:3000"
    restart: unless-stopped
